switch 表达式如果

switch（a）
{
    case 1常量：
          {     }
          break;
    case 2常量：
          {     }
         break：    
       ............
     default
          {    }
}
 



sum +=i;和sum=sum+i一样






do
    程序块
  while（条件）;  （ 注意要有这个分号 ）
      {  程序先执行一次之后在判断while里面的条件  }





int fact(int n)
{
      if(n<=1)
         return 1;
      else
         return(n*fact(n-1))
}

       类似迭代的函数求的阶乘，一直再返回fact（n），
所以最后返回到fact（1），再返回到1，来得到！


i++和++i 不一样，i++是先用i的值然后再加1，++i是先加一然后再用i的值，两者有区别。
先运算再自加，先自加再运算。

启动 		win+R，搜索cmd
切换盘		磁盘名称：
进入文件夹	cd 名称
进入多级文件夹	cd 名称1\2\3
返回上一级	cd \
查看当前内容	dir
清屏		cls
退出		exit









